const express = require('express');
const router = express.Router();
const items = require('./items');
// const db = require('../models');
// const Items = db.item;

// router.get('/all', (req, res) => {
//   return Items.findAll({include:[{model:}]})
//   .then
// })

module.exports = router;